query IssueBoard($id: ID!){
  issueBoard(issueBoardId:$id){
    id
    name
    issueLabels{
      id
      name
      color
      issues{
        id
        title
        issueId
        dueDate
      }
    }
  }
}
